// reducers.js
const initialState = {
    // L'état initial de votre application
  };
  
  const reducers = (state = initialState, action) => {
    switch (action.type) {
      // Définissez vos cas de reducers ici
      default:
        return state;
    }
  };
  
  export default reducers;
  